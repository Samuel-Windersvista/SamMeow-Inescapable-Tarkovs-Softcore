# AGENTS.md

Guidance for coding agents working in this repository.

## Project

Inescapable Tarkov's Softcore —— SPT 5.0 服务端整合 mod（六组功能 + 战局时长控制）。详见 `README.md` 与 `CONTEXT.md`（术语表）。

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues on `Samuel-Windersvista/SamMeow-Inescapable-Tarkovs-Softcore` (via `gh`). See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage roles, each label string equal to its role name (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` plus `docs/adr/` at the repo root. See `docs/agents/domain.md`.
