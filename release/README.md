# Release artifacts

Published release archives (modpack overlay bundles, versioned zips) are
archived here. Contents of this directory other than `.gitkeep` and this file
are ignored by git — the source of truth for each release is its git tag plus
the build output.

Convention: `[5]核心-Inescapable-Tarkovs-Softcore-<version>.zip`
